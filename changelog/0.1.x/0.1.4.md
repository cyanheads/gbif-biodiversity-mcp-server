---
summary: "Metadata alignment: Dockerfile OCI labels, package.json scripts/files/engines, manifest.json fields, .mcpbignore, README Docker badge and Bun version."
breaking: false
security: false
---

# 0.1.4 — 2026-05-23

## Changed

- **Dockerfile** — OCI `title` label uses scoped package name (`@cyanheads/gbif-mcp-server`); `description` label updated to match canonical tagline; label order normalized.
- **`package.json`** — `files[]` drops `changelog/` and `AGENTS.md` (not in dist); `rebuild` script chains via `bun run build`; `format` script drops trailing `.`; `test` uses `bunx vitest run`; `start:stdio` / `start:http` switch from `node` to `bun`; `engines.bun` pinned to `>=1.3.2`.
- **`manifest.json`** — `repository`, `homepage`, and `license` fields added; JSON formatted for readability.
- **`.mcpbignore`** — `.agents/` entry added.
- **README** — Docker badge added; Bun version badge updated to `v1.3.2`; prerequisites prose updated.
