---
summary: "First npm publish — agent-facing output improvements, code cleanup, and packaging metadata."
breaking: false
security: false
---

# 0.1.1 — 2026-05-23

## Changed

- **`gbif_search_occurrences`** — expanded Darwin Core filter set; restructured output fields for clearer agent-facing text mode.
- **`gbif_get_dataset`** — contact list now surfaces role, name, organization, and email in `format()` output.
- **`gbif_get_species_children`** and **`gbif_get_species_classification`** — tightened field descriptions and output rendering.
- **`gbif_search_datasets`** and **`gbif_search_publishers`** — description and output field alignment pass.
- **`gbif_occurrence_facets`**, **`gbif_get_occurrence`**, **`gbif_match_species`** — minor output and description refinements from agent-facing audit.
- **GBIF service layer** — internal request/response handling cleanup; no behavior change.
- **Packaging** — `server.json`, `manifest.json`, and README install badges added; `bundle` script wired for `.mcpb` output.
